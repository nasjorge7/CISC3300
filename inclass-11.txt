In class assignment 11
1. JQuery is a lightweight, "write less, do more", JavaScript library
2. JavaScript Library makes our lives easier when doing common HTML/CSS/JS tasks.
3. AJAX allows web pages to be updated asynchronously by exchanging data with a web
server behind the scenes.
4. Hyper Text Transfer Protocol (HTTP) is how the internet handles communication between clients and servers, is done by
requests and responses.
5. JSON is a text-based data format following JavaScript object syntax.
6. API’s is a way for two or more computer programs or components to communicate with
each other.
7. Create a webpage that (using jQuery) when a button is clicked an HTTP GET request is sent
to a PHP backend. From the PHP backend return an associative array that is JSON encoded.
Console.log the response on the front end.
